import { ResourceType } from './resource-type';

/**
 * The resource for BitstreamDownloadURL
 *
 * Needs to be in a separate file to prevent circular
 * dependencies in webpack.
 */
export const BITSTREAM_DIRECT_DOWNLOAD_URL = new ResourceType('bitstreamdirectdownloadurl');
