import { ResourceType } from './resource-type';

/**
 * The resource type for BitstreamAccessConditions
 *
 * Needs to be in a separate file to prevent circular
 * dependencies in webpack.
 */
export const BITSTREAM_ACCESS_CONDITIONS = new ResourceType('bitstreamaccesscondition');
